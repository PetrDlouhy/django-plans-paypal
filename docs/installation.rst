============
Installation
============

At the command line::

    $ easy_install django-plans-paypal

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv django-plans-paypal
    $ pip install django-plans-paypal
